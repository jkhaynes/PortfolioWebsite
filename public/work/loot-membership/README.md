# Loot membership app screenshots

Provided by Jessica for the portfolio case study on 2026-09-05.

- `tier-editor.png`: original tier editor capture, 900 × 780. No visible personal identifiers; role/tier names and the Shopify membership tag are product configuration.
- `customer-dashboard.png`: original dashboard capture, 959 × 448. Customer search is empty; aggregate counts describe the captured app state and are not impact metrics.
- `customer-connected-redacted.png`: original customer-account capture, 685 × 401, with opaque gray redactions covering the Discord username and verification timestamp. Exact pixel editing was explicitly authorized. Every pixel outside the two redaction rectangles was verified unchanged.

Redaction rectangles, in original pixel coordinates: `(83, 118, 60, 17)` and `(74, 148, 141, 18)` (x, y, width, height). The unredacted customer image is not stored in the repository. No AI-generated replacement screenshot is used.

Preserve these captures as app evidence. Do not relabel them as mockups or sample data; live rollout status is unconfirmed. Use the shared media dialog to inspect the complete images without the homepage card's crop.
